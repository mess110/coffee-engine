// Generated by CoffeeScript 1.9.3
var Config;

Config = (function() {
  var PrivateClass, instance;

  function Config() {}

  instance = null;

  PrivateClass = (function() {
    function PrivateClass() {
      this.showStatsOnLoad = false;
      this.contextMenuDisabled = true;
      this.antialias = true;
      this.anaglyph = false;
      this.anaglyphDistance = 600;
      this.resize = false;
      this.width = 1280;
      this.height = 720;
      this.soundEnabled = false;
      this.debug = false;
      this.preventDefaultMouseEvents = true;
    }

    PrivateClass.prototype.fillWindow = function() {
      this.resize = true;
      this.width = window.innerWidth;
      return this.height = window.innerHeight;
    };

    PrivateClass.prototype.toggleAnaglyph = function() {
      return this.anaglyph = !this.anaglyph;
    };

    PrivateClass.prototype.toggleStats = function() {
      return StatsManager.get().toggle();
    };

    PrivateClass.prototype.toggleSound = function() {
      return this.soundEnabled = !this.soundEnabled;
    };

    PrivateClass.prototype.toggleDebug = function() {
      return this.debug = !this.debug;
    };

    PrivateClass.prototype.toggleFullScreen = function() {
      return EngineUtils.toggleFullScreen();
    };

    return PrivateClass;

  })();

  Config.get = function() {
    return instance != null ? instance : instance = new PrivateClass();
  };

  return Config;

})();

exports.Config = Config;
